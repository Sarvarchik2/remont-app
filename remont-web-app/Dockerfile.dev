FROM node:20-slim

WORKDIR /app

RUN npm config set fetch-retries 5

COPY package*.json ./
RUN npm install

COPY . .

# Pass host argument so Vite listens on external network
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
